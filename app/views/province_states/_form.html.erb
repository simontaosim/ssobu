<%= form_for(@province_state) do |f| %>
  <% if @province_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@province_state.errors.count, "error") %> prohibited this province_state from being saved:</h2>

      <ul>
      <% @province_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
